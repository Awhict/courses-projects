# 正则表达式（未完待续）
import re                       # 导入库
content = "1234567890"          # 定义变量
p = re.compile()                # 定义模式
for one in p.finfall(content):  # 遍历文字
    print(one)                  # 得到结果
